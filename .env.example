# 夢想職人照：兩版本各自使用不同 API 金鑰（複製此檔為 .env 並填入，勿提交 .env）
#
# 第二版（快版／純 Gemini）：/career_photo_fast → 使用 GEMINI_API_KEY
# 第一版（Replicate SAM+InstantID）：/career_photo → 使用 REPLICATE_API_TOKEN
# 兩版網頁操作相同，僅 AI 生成方式不同。
#
GEMINI_API_KEY=
REPLICATE_API_TOKEN=
